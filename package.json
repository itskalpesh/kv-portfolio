{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "corepack pnpm --filter @workspace/portfolio dev",
    "build": "corepack pnpm --filter @workspace/portfolio build",
    "typecheck": "corepack pnpm --filter @workspace/portfolio typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
