{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
    "scripts": {
    "dev": "pnpm --filter @workspace/portfolio dev",
    "build": "pnpm --filter @workspace/portfolio build",
    "typecheck": "pnpm --filter @workspace/portfolio typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
